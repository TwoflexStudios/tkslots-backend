// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.33.0
// source: pb_lobby_broadcast.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "pb_lobby_broadcast";

export interface notifyGameNoticeMsg {
  id: number;
  nickname: string;
  win: number;
  gameid: number;
  roomid: number;
  time: number;
  tag: number;
}

export interface notifyNoticeListMsg {
  noticelist: notifyGameNoticeMsg[];
}

export interface scJackpotWinnerMsg {
  winnerlist: notifyGameNoticeMsg[];
}

export interface redPoint {
  tag: string;
  num: number;
}

export interface scRedPoints {
  points: redPoint[];
}

function createBasenotifyGameNoticeMsg(): notifyGameNoticeMsg {
  return { id: 0, nickname: "", win: 0, gameid: 0, roomid: 0, time: 0, tag: 0 };
}

export const notifyGameNoticeMsg: MessageFns<notifyGameNoticeMsg> = {
  encode(message: notifyGameNoticeMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    if (message.nickname !== "") {
      writer.uint32(18).string(message.nickname);
    }
    if (message.win !== 0) {
      writer.uint32(24).int64(message.win);
    }
    if (message.gameid !== 0) {
      writer.uint32(32).int32(message.gameid);
    }
    if (message.roomid !== 0) {
      writer.uint32(40).int32(message.roomid);
    }
    if (message.time !== 0) {
      writer.uint32(48).int32(message.time);
    }
    if (message.tag !== 0) {
      writer.uint32(56).int32(message.tag);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): notifyGameNoticeMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasenotifyGameNoticeMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nickname = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.win = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.gameid = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.roomid = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.time = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.tag = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): notifyGameNoticeMsg {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      nickname: isSet(object.nickname) ? globalThis.String(object.nickname) : "",
      win: isSet(object.win) ? globalThis.Number(object.win) : 0,
      gameid: isSet(object.gameid) ? globalThis.Number(object.gameid) : 0,
      roomid: isSet(object.roomid) ? globalThis.Number(object.roomid) : 0,
      time: isSet(object.time) ? globalThis.Number(object.time) : 0,
      tag: isSet(object.tag) ? globalThis.Number(object.tag) : 0,
    };
  },

  toJSON(message: notifyGameNoticeMsg): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.nickname !== "") {
      obj.nickname = message.nickname;
    }
    if (message.win !== 0) {
      obj.win = Math.round(message.win);
    }
    if (message.gameid !== 0) {
      obj.gameid = Math.round(message.gameid);
    }
    if (message.roomid !== 0) {
      obj.roomid = Math.round(message.roomid);
    }
    if (message.time !== 0) {
      obj.time = Math.round(message.time);
    }
    if (message.tag !== 0) {
      obj.tag = Math.round(message.tag);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<notifyGameNoticeMsg>, I>>(base?: I): notifyGameNoticeMsg {
    return notifyGameNoticeMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<notifyGameNoticeMsg>, I>>(object: I): notifyGameNoticeMsg {
    const message = createBasenotifyGameNoticeMsg();
    message.id = object.id ?? 0;
    message.nickname = object.nickname ?? "";
    message.win = object.win ?? 0;
    message.gameid = object.gameid ?? 0;
    message.roomid = object.roomid ?? 0;
    message.time = object.time ?? 0;
    message.tag = object.tag ?? 0;
    return message;
  },
};

function createBasenotifyNoticeListMsg(): notifyNoticeListMsg {
  return { noticelist: [] };
}

export const notifyNoticeListMsg: MessageFns<notifyNoticeListMsg> = {
  encode(message: notifyNoticeListMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.noticelist) {
      notifyGameNoticeMsg.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): notifyNoticeListMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasenotifyNoticeListMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.noticelist.push(notifyGameNoticeMsg.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): notifyNoticeListMsg {
    return {
      noticelist: globalThis.Array.isArray(object?.noticelist)
        ? object.noticelist.map((e: any) => notifyGameNoticeMsg.fromJSON(e))
        : [],
    };
  },

  toJSON(message: notifyNoticeListMsg): unknown {
    const obj: any = {};
    if (message.noticelist?.length) {
      obj.noticelist = message.noticelist.map((e) => notifyGameNoticeMsg.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<notifyNoticeListMsg>, I>>(base?: I): notifyNoticeListMsg {
    return notifyNoticeListMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<notifyNoticeListMsg>, I>>(object: I): notifyNoticeListMsg {
    const message = createBasenotifyNoticeListMsg();
    message.noticelist = object.noticelist?.map((e) => notifyGameNoticeMsg.fromPartial(e)) || [];
    return message;
  },
};

function createBasescJackpotWinnerMsg(): scJackpotWinnerMsg {
  return { winnerlist: [] };
}

export const scJackpotWinnerMsg: MessageFns<scJackpotWinnerMsg> = {
  encode(message: scJackpotWinnerMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.winnerlist) {
      notifyGameNoticeMsg.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): scJackpotWinnerMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasescJackpotWinnerMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.winnerlist.push(notifyGameNoticeMsg.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): scJackpotWinnerMsg {
    return {
      winnerlist: globalThis.Array.isArray(object?.winnerlist)
        ? object.winnerlist.map((e: any) => notifyGameNoticeMsg.fromJSON(e))
        : [],
    };
  },

  toJSON(message: scJackpotWinnerMsg): unknown {
    const obj: any = {};
    if (message.winnerlist?.length) {
      obj.winnerlist = message.winnerlist.map((e) => notifyGameNoticeMsg.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<scJackpotWinnerMsg>, I>>(base?: I): scJackpotWinnerMsg {
    return scJackpotWinnerMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<scJackpotWinnerMsg>, I>>(object: I): scJackpotWinnerMsg {
    const message = createBasescJackpotWinnerMsg();
    message.winnerlist = object.winnerlist?.map((e) => notifyGameNoticeMsg.fromPartial(e)) || [];
    return message;
  },
};

function createBaseredPoint(): redPoint {
  return { tag: "", num: 0 };
}

export const redPoint: MessageFns<redPoint> = {
  encode(message: redPoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tag !== "") {
      writer.uint32(10).string(message.tag);
    }
    if (message.num !== 0) {
      writer.uint32(16).int32(message.num);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): redPoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseredPoint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tag = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.num = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): redPoint {
    return {
      tag: isSet(object.tag) ? globalThis.String(object.tag) : "",
      num: isSet(object.num) ? globalThis.Number(object.num) : 0,
    };
  },

  toJSON(message: redPoint): unknown {
    const obj: any = {};
    if (message.tag !== "") {
      obj.tag = message.tag;
    }
    if (message.num !== 0) {
      obj.num = Math.round(message.num);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<redPoint>, I>>(base?: I): redPoint {
    return redPoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<redPoint>, I>>(object: I): redPoint {
    const message = createBaseredPoint();
    message.tag = object.tag ?? "";
    message.num = object.num ?? 0;
    return message;
  },
};

function createBasescRedPoints(): scRedPoints {
  return { points: [] };
}

export const scRedPoints: MessageFns<scRedPoints> = {
  encode(message: scRedPoints, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.points) {
      redPoint.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): scRedPoints {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasescRedPoints();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.points.push(redPoint.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): scRedPoints {
    return {
      points: globalThis.Array.isArray(object?.points) ? object.points.map((e: any) => redPoint.fromJSON(e)) : [],
    };
  },

  toJSON(message: scRedPoints): unknown {
    const obj: any = {};
    if (message.points?.length) {
      obj.points = message.points.map((e) => redPoint.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<scRedPoints>, I>>(base?: I): scRedPoints {
    return scRedPoints.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<scRedPoints>, I>>(object: I): scRedPoints {
    const message = createBasescRedPoints();
    message.points = object.points?.map((e) => redPoint.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
