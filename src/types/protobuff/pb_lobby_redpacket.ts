// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.33.0
// source: pb_lobby_redpacket.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "pb_lobby_redpacket";

export interface scShowbeforeMsg {
  begintime: number;
  starttime: number;
  endtime: number;
  status: number;
  money: number;
}

export interface scRedpacket {
  money: number[];
  playtime: number;
}

export interface csRedpacketOver {
  money: number;
}

export interface scRedpacketOver {
  cmoney: number;
}

function createBasescShowbeforeMsg(): scShowbeforeMsg {
  return { begintime: 0, starttime: 0, endtime: 0, status: 0, money: 0 };
}

export const scShowbeforeMsg: MessageFns<scShowbeforeMsg> = {
  encode(message: scShowbeforeMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.begintime !== 0) {
      writer.uint32(8).int64(message.begintime);
    }
    if (message.starttime !== 0) {
      writer.uint32(16).int64(message.starttime);
    }
    if (message.endtime !== 0) {
      writer.uint32(24).int64(message.endtime);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    if (message.money !== 0) {
      writer.uint32(40).int64(message.money);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): scShowbeforeMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasescShowbeforeMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.begintime = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.starttime = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.endtime = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.money = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): scShowbeforeMsg {
    return {
      begintime: isSet(object.begintime) ? globalThis.Number(object.begintime) : 0,
      starttime: isSet(object.starttime) ? globalThis.Number(object.starttime) : 0,
      endtime: isSet(object.endtime) ? globalThis.Number(object.endtime) : 0,
      status: isSet(object.status) ? globalThis.Number(object.status) : 0,
      money: isSet(object.money) ? globalThis.Number(object.money) : 0,
    };
  },

  toJSON(message: scShowbeforeMsg): unknown {
    const obj: any = {};
    if (message.begintime !== 0) {
      obj.begintime = Math.round(message.begintime);
    }
    if (message.starttime !== 0) {
      obj.starttime = Math.round(message.starttime);
    }
    if (message.endtime !== 0) {
      obj.endtime = Math.round(message.endtime);
    }
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    if (message.money !== 0) {
      obj.money = Math.round(message.money);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<scShowbeforeMsg>, I>>(base?: I): scShowbeforeMsg {
    return scShowbeforeMsg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<scShowbeforeMsg>, I>>(object: I): scShowbeforeMsg {
    const message = createBasescShowbeforeMsg();
    message.begintime = object.begintime ?? 0;
    message.starttime = object.starttime ?? 0;
    message.endtime = object.endtime ?? 0;
    message.status = object.status ?? 0;
    message.money = object.money ?? 0;
    return message;
  },
};

function createBasescRedpacket(): scRedpacket {
  return { money: [], playtime: 0 };
}

export const scRedpacket: MessageFns<scRedpacket> = {
  encode(message: scRedpacket, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.money) {
      writer.int64(v);
    }
    writer.join();
    if (message.playtime !== 0) {
      writer.uint32(16).int32(message.playtime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): scRedpacket {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasescRedpacket();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.money.push(longToNumber(reader.int64()));

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.money.push(longToNumber(reader.int64()));
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.playtime = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): scRedpacket {
    return {
      money: globalThis.Array.isArray(object?.money) ? object.money.map((e: any) => globalThis.Number(e)) : [],
      playtime: isSet(object.playtime) ? globalThis.Number(object.playtime) : 0,
    };
  },

  toJSON(message: scRedpacket): unknown {
    const obj: any = {};
    if (message.money?.length) {
      obj.money = message.money.map((e) => Math.round(e));
    }
    if (message.playtime !== 0) {
      obj.playtime = Math.round(message.playtime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<scRedpacket>, I>>(base?: I): scRedpacket {
    return scRedpacket.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<scRedpacket>, I>>(object: I): scRedpacket {
    const message = createBasescRedpacket();
    message.money = object.money?.map((e) => e) || [];
    message.playtime = object.playtime ?? 0;
    return message;
  },
};

function createBasecsRedpacketOver(): csRedpacketOver {
  return { money: 0 };
}

export const csRedpacketOver: MessageFns<csRedpacketOver> = {
  encode(message: csRedpacketOver, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.money !== 0) {
      writer.uint32(8).int64(message.money);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): csRedpacketOver {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasecsRedpacketOver();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.money = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): csRedpacketOver {
    return { money: isSet(object.money) ? globalThis.Number(object.money) : 0 };
  },

  toJSON(message: csRedpacketOver): unknown {
    const obj: any = {};
    if (message.money !== 0) {
      obj.money = Math.round(message.money);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<csRedpacketOver>, I>>(base?: I): csRedpacketOver {
    return csRedpacketOver.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<csRedpacketOver>, I>>(object: I): csRedpacketOver {
    const message = createBasecsRedpacketOver();
    message.money = object.money ?? 0;
    return message;
  },
};

function createBasescRedpacketOver(): scRedpacketOver {
  return { cmoney: 0 };
}

export const scRedpacketOver: MessageFns<scRedpacketOver> = {
  encode(message: scRedpacketOver, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cmoney !== 0) {
      writer.uint32(8).int64(message.cmoney);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): scRedpacketOver {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasescRedpacketOver();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.cmoney = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): scRedpacketOver {
    return { cmoney: isSet(object.cmoney) ? globalThis.Number(object.cmoney) : 0 };
  },

  toJSON(message: scRedpacketOver): unknown {
    const obj: any = {};
    if (message.cmoney !== 0) {
      obj.cmoney = Math.round(message.cmoney);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<scRedpacketOver>, I>>(base?: I): scRedpacketOver {
    return scRedpacketOver.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<scRedpacketOver>, I>>(object: I): scRedpacketOver {
    const message = createBasescRedpacketOver();
    message.cmoney = object.cmoney ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
