// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.33.0
// source: rabbit_fortune.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "pb_game_100031";

export interface csGameRoll {
  betScore: number;
}

export interface scGameRoll {
  currentMode: number;
  nextMode: number;
  winScore: number;
  winType: number;
  userScore: number;
  betScore: number;
  resultCells: number[];
  resultLines: number[];
  hitCells: number[];
  rotatestate: number;
  muti: number[];
  mutiHit: number[];
  ratatetimes: number;
  totalrotatetimes: number;
}

export interface scGameInfo {
  gameinfo: scGameRoll | undefined;
  gameLine: number;
  defaultBet: number;
}

function createBasecsGameRoll(): csGameRoll {
  return { betScore: 0 };
}

export const csGameRoll: MessageFns<csGameRoll> = {
  encode(message: csGameRoll, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.betScore !== 0) {
      writer.uint32(8).int64(message.betScore);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): csGameRoll {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasecsGameRoll();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.betScore = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): csGameRoll {
    return { betScore: isSet(object.betScore) ? globalThis.Number(object.betScore) : 0 };
  },

  toJSON(message: csGameRoll): unknown {
    const obj: any = {};
    if (message.betScore !== 0) {
      obj.betScore = Math.round(message.betScore);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<csGameRoll>, I>>(base?: I): csGameRoll {
    return csGameRoll.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<csGameRoll>, I>>(object: I): csGameRoll {
    const message = createBasecsGameRoll();
    message.betScore = object.betScore ?? 0;
    return message;
  },
};

function createBasescGameRoll(): scGameRoll {
  return {
    currentMode: 0,
    nextMode: 0,
    winScore: 0,
    winType: 0,
    userScore: 0,
    betScore: 0,
    resultCells: [],
    resultLines: [],
    hitCells: [],
    rotatestate: 0,
    muti: [],
    mutiHit: [],
    ratatetimes: 0,
    totalrotatetimes: 0,
  };
}

export const scGameRoll: MessageFns<scGameRoll> = {
  encode(message: scGameRoll, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.currentMode !== 0) {
      writer.uint32(8).int32(message.currentMode);
    }
    if (message.nextMode !== 0) {
      writer.uint32(16).int32(message.nextMode);
    }
    if (message.winScore !== 0) {
      writer.uint32(24).int64(message.winScore);
    }
    if (message.winType !== 0) {
      writer.uint32(32).int32(message.winType);
    }
    if (message.userScore !== 0) {
      writer.uint32(40).int64(message.userScore);
    }
    if (message.betScore !== 0) {
      writer.uint32(48).int64(message.betScore);
    }
    writer.uint32(58).fork();
    for (const v of message.resultCells) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(66).fork();
    for (const v of message.resultLines) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(74).fork();
    for (const v of message.hitCells) {
      writer.int32(v);
    }
    writer.join();
    if (message.rotatestate !== 0) {
      writer.uint32(80).int32(message.rotatestate);
    }
    writer.uint32(90).fork();
    for (const v of message.muti) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(98).fork();
    for (const v of message.mutiHit) {
      writer.int32(v);
    }
    writer.join();
    if (message.ratatetimes !== 0) {
      writer.uint32(104).int32(message.ratatetimes);
    }
    if (message.totalrotatetimes !== 0) {
      writer.uint32(112).int32(message.totalrotatetimes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): scGameRoll {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasescGameRoll();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.currentMode = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.nextMode = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.winScore = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.winType = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.userScore = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.betScore = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag === 56) {
            message.resultCells.push(reader.int32());

            continue;
          }

          if (tag === 58) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.resultCells.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 8: {
          if (tag === 64) {
            message.resultLines.push(reader.int32());

            continue;
          }

          if (tag === 66) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.resultLines.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 9: {
          if (tag === 72) {
            message.hitCells.push(reader.int32());

            continue;
          }

          if (tag === 74) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.hitCells.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.rotatestate = reader.int32();
          continue;
        }
        case 11: {
          if (tag === 88) {
            message.muti.push(reader.int32());

            continue;
          }

          if (tag === 90) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.muti.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 12: {
          if (tag === 96) {
            message.mutiHit.push(reader.int32());

            continue;
          }

          if (tag === 98) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.mutiHit.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.ratatetimes = reader.int32();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.totalrotatetimes = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): scGameRoll {
    return {
      currentMode: isSet(object.currentMode) ? globalThis.Number(object.currentMode) : 0,
      nextMode: isSet(object.nextMode) ? globalThis.Number(object.nextMode) : 0,
      winScore: isSet(object.winScore) ? globalThis.Number(object.winScore) : 0,
      winType: isSet(object.winType) ? globalThis.Number(object.winType) : 0,
      userScore: isSet(object.userScore) ? globalThis.Number(object.userScore) : 0,
      betScore: isSet(object.betScore) ? globalThis.Number(object.betScore) : 0,
      resultCells: globalThis.Array.isArray(object?.resultCells)
        ? object.resultCells.map((e: any) => globalThis.Number(e))
        : [],
      resultLines: globalThis.Array.isArray(object?.resultLines)
        ? object.resultLines.map((e: any) => globalThis.Number(e))
        : [],
      hitCells: globalThis.Array.isArray(object?.hitCells) ? object.hitCells.map((e: any) => globalThis.Number(e)) : [],
      rotatestate: isSet(object.rotatestate) ? globalThis.Number(object.rotatestate) : 0,
      muti: globalThis.Array.isArray(object?.muti) ? object.muti.map((e: any) => globalThis.Number(e)) : [],
      mutiHit: globalThis.Array.isArray(object?.mutiHit) ? object.mutiHit.map((e: any) => globalThis.Number(e)) : [],
      ratatetimes: isSet(object.ratatetimes) ? globalThis.Number(object.ratatetimes) : 0,
      totalrotatetimes: isSet(object.totalrotatetimes) ? globalThis.Number(object.totalrotatetimes) : 0,
    };
  },

  toJSON(message: scGameRoll): unknown {
    const obj: any = {};
    if (message.currentMode !== 0) {
      obj.currentMode = Math.round(message.currentMode);
    }
    if (message.nextMode !== 0) {
      obj.nextMode = Math.round(message.nextMode);
    }
    if (message.winScore !== 0) {
      obj.winScore = Math.round(message.winScore);
    }
    if (message.winType !== 0) {
      obj.winType = Math.round(message.winType);
    }
    if (message.userScore !== 0) {
      obj.userScore = Math.round(message.userScore);
    }
    if (message.betScore !== 0) {
      obj.betScore = Math.round(message.betScore);
    }
    if (message.resultCells?.length) {
      obj.resultCells = message.resultCells.map((e) => Math.round(e));
    }
    if (message.resultLines?.length) {
      obj.resultLines = message.resultLines.map((e) => Math.round(e));
    }
    if (message.hitCells?.length) {
      obj.hitCells = message.hitCells.map((e) => Math.round(e));
    }
    if (message.rotatestate !== 0) {
      obj.rotatestate = Math.round(message.rotatestate);
    }
    if (message.muti?.length) {
      obj.muti = message.muti.map((e) => Math.round(e));
    }
    if (message.mutiHit?.length) {
      obj.mutiHit = message.mutiHit.map((e) => Math.round(e));
    }
    if (message.ratatetimes !== 0) {
      obj.ratatetimes = Math.round(message.ratatetimes);
    }
    if (message.totalrotatetimes !== 0) {
      obj.totalrotatetimes = Math.round(message.totalrotatetimes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<scGameRoll>, I>>(base?: I): scGameRoll {
    return scGameRoll.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<scGameRoll>, I>>(object: I): scGameRoll {
    const message = createBasescGameRoll();
    message.currentMode = object.currentMode ?? 0;
    message.nextMode = object.nextMode ?? 0;
    message.winScore = object.winScore ?? 0;
    message.winType = object.winType ?? 0;
    message.userScore = object.userScore ?? 0;
    message.betScore = object.betScore ?? 0;
    message.resultCells = object.resultCells?.map((e) => e) || [];
    message.resultLines = object.resultLines?.map((e) => e) || [];
    message.hitCells = object.hitCells?.map((e) => e) || [];
    message.rotatestate = object.rotatestate ?? 0;
    message.muti = object.muti?.map((e) => e) || [];
    message.mutiHit = object.mutiHit?.map((e) => e) || [];
    message.ratatetimes = object.ratatetimes ?? 0;
    message.totalrotatetimes = object.totalrotatetimes ?? 0;
    return message;
  },
};

function createBasescGameInfo(): scGameInfo {
  return { gameinfo: undefined, gameLine: 0, defaultBet: 0 };
}

export const scGameInfo: MessageFns<scGameInfo> = {
  encode(message: scGameInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameinfo !== undefined) {
      scGameRoll.encode(message.gameinfo, writer.uint32(10).fork()).join();
    }
    if (message.gameLine !== 0) {
      writer.uint32(16).int32(message.gameLine);
    }
    if (message.defaultBet !== 0) {
      writer.uint32(24).int64(message.defaultBet);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): scGameInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasescGameInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameinfo = scGameRoll.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.gameLine = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.defaultBet = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): scGameInfo {
    return {
      gameinfo: isSet(object.gameinfo) ? scGameRoll.fromJSON(object.gameinfo) : undefined,
      gameLine: isSet(object.gameLine) ? globalThis.Number(object.gameLine) : 0,
      defaultBet: isSet(object.defaultBet) ? globalThis.Number(object.defaultBet) : 0,
    };
  },

  toJSON(message: scGameInfo): unknown {
    const obj: any = {};
    if (message.gameinfo !== undefined) {
      obj.gameinfo = scGameRoll.toJSON(message.gameinfo);
    }
    if (message.gameLine !== 0) {
      obj.gameLine = Math.round(message.gameLine);
    }
    if (message.defaultBet !== 0) {
      obj.defaultBet = Math.round(message.defaultBet);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<scGameInfo>, I>>(base?: I): scGameInfo {
    return scGameInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<scGameInfo>, I>>(object: I): scGameInfo {
    const message = createBasescGameInfo();
    message.gameinfo = (object.gameinfo !== undefined && object.gameinfo !== null)
      ? scGameRoll.fromPartial(object.gameinfo)
      : undefined;
    message.gameLine = object.gameLine ?? 0;
    message.defaultBet = object.defaultBet ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
