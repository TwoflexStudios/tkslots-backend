// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.33.0
// source: pb_lobby_rank.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "pb_lobby_rank";

export interface PlayerProfit {
  rank: number;
  nickname: string;
  faceid: number;
  viplevel: number;
  profit: number;
}

export interface PlayerLuck {
  rank: number;
  nickname: string;
  faceid: number;
  viplevel: number;
  gameid: number;
  win: number;
  time: number;
}

export interface scRankProfit {
  my: PlayerProfit | undefined;
  ranks: PlayerProfit[];
}

export interface scRankLuck {
  my: PlayerLuck | undefined;
  ranks: PlayerLuck[];
}

function createBasePlayerProfit(): PlayerProfit {
  return { rank: 0, nickname: "", faceid: 0, viplevel: 0, profit: 0 };
}

export const PlayerProfit: MessageFns<PlayerProfit> = {
  encode(message: PlayerProfit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rank !== 0) {
      writer.uint32(8).int32(message.rank);
    }
    if (message.nickname !== "") {
      writer.uint32(18).string(message.nickname);
    }
    if (message.faceid !== 0) {
      writer.uint32(24).int32(message.faceid);
    }
    if (message.viplevel !== 0) {
      writer.uint32(32).int32(message.viplevel);
    }
    if (message.profit !== 0) {
      writer.uint32(40).int64(message.profit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerProfit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerProfit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.rank = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nickname = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.faceid = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.viplevel = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.profit = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerProfit {
    return {
      rank: isSet(object.rank) ? globalThis.Number(object.rank) : 0,
      nickname: isSet(object.nickname) ? globalThis.String(object.nickname) : "",
      faceid: isSet(object.faceid) ? globalThis.Number(object.faceid) : 0,
      viplevel: isSet(object.viplevel) ? globalThis.Number(object.viplevel) : 0,
      profit: isSet(object.profit) ? globalThis.Number(object.profit) : 0,
    };
  },

  toJSON(message: PlayerProfit): unknown {
    const obj: any = {};
    if (message.rank !== 0) {
      obj.rank = Math.round(message.rank);
    }
    if (message.nickname !== "") {
      obj.nickname = message.nickname;
    }
    if (message.faceid !== 0) {
      obj.faceid = Math.round(message.faceid);
    }
    if (message.viplevel !== 0) {
      obj.viplevel = Math.round(message.viplevel);
    }
    if (message.profit !== 0) {
      obj.profit = Math.round(message.profit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerProfit>, I>>(base?: I): PlayerProfit {
    return PlayerProfit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerProfit>, I>>(object: I): PlayerProfit {
    const message = createBasePlayerProfit();
    message.rank = object.rank ?? 0;
    message.nickname = object.nickname ?? "";
    message.faceid = object.faceid ?? 0;
    message.viplevel = object.viplevel ?? 0;
    message.profit = object.profit ?? 0;
    return message;
  },
};

function createBasePlayerLuck(): PlayerLuck {
  return { rank: 0, nickname: "", faceid: 0, viplevel: 0, gameid: 0, win: 0, time: 0 };
}

export const PlayerLuck: MessageFns<PlayerLuck> = {
  encode(message: PlayerLuck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rank !== 0) {
      writer.uint32(8).int32(message.rank);
    }
    if (message.nickname !== "") {
      writer.uint32(18).string(message.nickname);
    }
    if (message.faceid !== 0) {
      writer.uint32(24).int32(message.faceid);
    }
    if (message.viplevel !== 0) {
      writer.uint32(32).int32(message.viplevel);
    }
    if (message.gameid !== 0) {
      writer.uint32(40).int32(message.gameid);
    }
    if (message.win !== 0) {
      writer.uint32(48).int64(message.win);
    }
    if (message.time !== 0) {
      writer.uint32(56).int64(message.time);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerLuck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerLuck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.rank = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nickname = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.faceid = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.viplevel = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.gameid = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.win = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.time = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerLuck {
    return {
      rank: isSet(object.rank) ? globalThis.Number(object.rank) : 0,
      nickname: isSet(object.nickname) ? globalThis.String(object.nickname) : "",
      faceid: isSet(object.faceid) ? globalThis.Number(object.faceid) : 0,
      viplevel: isSet(object.viplevel) ? globalThis.Number(object.viplevel) : 0,
      gameid: isSet(object.gameid) ? globalThis.Number(object.gameid) : 0,
      win: isSet(object.win) ? globalThis.Number(object.win) : 0,
      time: isSet(object.time) ? globalThis.Number(object.time) : 0,
    };
  },

  toJSON(message: PlayerLuck): unknown {
    const obj: any = {};
    if (message.rank !== 0) {
      obj.rank = Math.round(message.rank);
    }
    if (message.nickname !== "") {
      obj.nickname = message.nickname;
    }
    if (message.faceid !== 0) {
      obj.faceid = Math.round(message.faceid);
    }
    if (message.viplevel !== 0) {
      obj.viplevel = Math.round(message.viplevel);
    }
    if (message.gameid !== 0) {
      obj.gameid = Math.round(message.gameid);
    }
    if (message.win !== 0) {
      obj.win = Math.round(message.win);
    }
    if (message.time !== 0) {
      obj.time = Math.round(message.time);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerLuck>, I>>(base?: I): PlayerLuck {
    return PlayerLuck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerLuck>, I>>(object: I): PlayerLuck {
    const message = createBasePlayerLuck();
    message.rank = object.rank ?? 0;
    message.nickname = object.nickname ?? "";
    message.faceid = object.faceid ?? 0;
    message.viplevel = object.viplevel ?? 0;
    message.gameid = object.gameid ?? 0;
    message.win = object.win ?? 0;
    message.time = object.time ?? 0;
    return message;
  },
};

function createBasescRankProfit(): scRankProfit {
  return { my: undefined, ranks: [] };
}

export const scRankProfit: MessageFns<scRankProfit> = {
  encode(message: scRankProfit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.my !== undefined) {
      PlayerProfit.encode(message.my, writer.uint32(10).fork()).join();
    }
    for (const v of message.ranks) {
      PlayerProfit.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): scRankProfit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasescRankProfit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.my = PlayerProfit.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ranks.push(PlayerProfit.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): scRankProfit {
    return {
      my: isSet(object.my) ? PlayerProfit.fromJSON(object.my) : undefined,
      ranks: globalThis.Array.isArray(object?.ranks) ? object.ranks.map((e: any) => PlayerProfit.fromJSON(e)) : [],
    };
  },

  toJSON(message: scRankProfit): unknown {
    const obj: any = {};
    if (message.my !== undefined) {
      obj.my = PlayerProfit.toJSON(message.my);
    }
    if (message.ranks?.length) {
      obj.ranks = message.ranks.map((e) => PlayerProfit.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<scRankProfit>, I>>(base?: I): scRankProfit {
    return scRankProfit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<scRankProfit>, I>>(object: I): scRankProfit {
    const message = createBasescRankProfit();
    message.my = (object.my !== undefined && object.my !== null) ? PlayerProfit.fromPartial(object.my) : undefined;
    message.ranks = object.ranks?.map((e) => PlayerProfit.fromPartial(e)) || [];
    return message;
  },
};

function createBasescRankLuck(): scRankLuck {
  return { my: undefined, ranks: [] };
}

export const scRankLuck: MessageFns<scRankLuck> = {
  encode(message: scRankLuck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.my !== undefined) {
      PlayerLuck.encode(message.my, writer.uint32(10).fork()).join();
    }
    for (const v of message.ranks) {
      PlayerLuck.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): scRankLuck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasescRankLuck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.my = PlayerLuck.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ranks.push(PlayerLuck.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): scRankLuck {
    return {
      my: isSet(object.my) ? PlayerLuck.fromJSON(object.my) : undefined,
      ranks: globalThis.Array.isArray(object?.ranks) ? object.ranks.map((e: any) => PlayerLuck.fromJSON(e)) : [],
    };
  },

  toJSON(message: scRankLuck): unknown {
    const obj: any = {};
    if (message.my !== undefined) {
      obj.my = PlayerLuck.toJSON(message.my);
    }
    if (message.ranks?.length) {
      obj.ranks = message.ranks.map((e) => PlayerLuck.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<scRankLuck>, I>>(base?: I): scRankLuck {
    return scRankLuck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<scRankLuck>, I>>(object: I): scRankLuck {
    const message = createBasescRankLuck();
    message.my = (object.my !== undefined && object.my !== null) ? PlayerLuck.fromPartial(object.my) : undefined;
    message.ranks = object.ranks?.map((e) => PlayerLuck.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
